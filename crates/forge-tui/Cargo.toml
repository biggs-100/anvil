[package]
name = "forge-tui"
version = "0.1.0"
edition = "2021"
description = "Forge TUI — terminal dashboard for forge environments"

[dependencies]
forge-core = { path = "../forge-core" }
ratatui = "0.28"
crossterm = "0.28"
tokio = { version = "1.35", features = ["full"] }
