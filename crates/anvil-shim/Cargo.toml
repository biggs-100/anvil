[package]
name = "anvil-shim"
version = "0.1.0"
edition = "2021"
description = "Anvil runtime shim — transparent binary forwarding with environment isolation"

[dependencies]
# No external dependencies to keep overhead minimal (<5ms)
