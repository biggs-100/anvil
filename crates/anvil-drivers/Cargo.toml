[package]
name = "anvil-drivers"
version = "0.1.0"
edition = "2021"
description = "Anvil drivers — standard command runners for managed runtimes"

[dependencies]
# No external dependencies needed for standard command runner drivers.
